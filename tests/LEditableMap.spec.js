// import { shallowMount } from "@vue/test-utils";
// import Vue from "vue";

import { LEditableMap } from "@/main";

describe.skip("LEditableMap", () => {
  it("has a created hook", () => {
    expect(typeof LEditableMap.created).toBe("function");
  });
  jest;
});
